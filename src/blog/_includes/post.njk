<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{ title }} | Kianoland Group</title>
  <meta name="description" content="{{ summary }}">
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <main style="max-width:800px;margin:2rem auto;padding:1rem;">
    <h1>{{ title }}</h1>
    <p style="color:#888;">{{ date | readableDate }}</p>
    {% if thumbnail %}
      <img src="{{ thumbnail }}" alt="{{ title }}" style="width:100%;border-radius:8px;margin:1rem 0;">
    {% endif %}
    <article>
      {{ content | safe }}
    </article>
  </main>
</body>
</html>
